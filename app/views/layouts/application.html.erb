<!DOCTYPE html>
<html>
  <head>
    <title>Culinary</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

  </head>

  <body>
    <header>
        <nav>         
          <%= link_to 'Students', students_path, :class => 'top-link' %> 
          <%= link_to 'Modules', course_modules_path, :class => 'top-link' %> 
        <% if true %> 

          <%= link_to 'Sign Out', session_path(1), :class => 'top-link', :id => 'top-link-ender' %>
          <% else %>
          <%= link_to 'Sign In', new_session_path, :class => 'top-link', :id => 'top-link-ender'  %>
        <% end %>
      </nav>
    </header>
    <main>
      <%= yield %>
    </main>
  </body>
</html>
